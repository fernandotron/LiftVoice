{
  "name": "liftvoice",
  "version": "1.0.0",
  "description": "LiftVoice - Real-time AI Simultaneous Audio Interpretation & Streaming",
  "private": true,
  "scripts": {
    "dev": "node dev.js",
    "dev:portless": "portless liftvoice -- node dev.js",
    "dev:mobile": "node dev.js",
    "dev:tunnel": "node dev.js --tunnel",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build:client": "npm --prefix client run build",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "install:all": "npm --prefix client install && npm --prefix server install",
    "start": "node server/src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "form-data": "^4.0.2",
    "localtunnel": "^2.0.2",
    "ws": "^8.18.0"
  }
}
